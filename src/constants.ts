// Restrict the number of books fetched from the Open Library.
export const LIMIT = 12;

// book cover sizes: 'S', 'M' and 'L'
export const COVER_SIZE = 'L';

// book title maximum length
export const MAX_LENGTH = 30;
